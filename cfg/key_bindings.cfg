exec modules/scripts/explode_n_kill

//  Custom actions
bind KP_MULTIPLY noop // Leave blank for Nvidia clip recording
bind KP_MINUS "-attack; -attack2; echo ▶ Cancel attacks;"
bind KP_ENTER "explode_n_kill"
// bind "b" sayCool
bind "h" sayGG
bind "m" saySrs
bind "n" retry
bind tab +scores

//  Default actions
bind "f" "+use_action_slot_item"
bind "i" "+inspect"
bind "/" "open_charinfo_direct"
bind F1 "vote option1"
bind F2 "vote option2"
bind F3 "vote option3; show_quest_log;"
bind "'" "+moveup"
bind SEMICOLON "+movedown"
bind ` +voicerecord
// bind ` toggleconsole // Use ` for binding if console fails to open from the homescreen
bind F11 toggleconsole

bindtoggle t r_drawviewmodel
