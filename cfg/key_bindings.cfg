//  Custom actions
bind "KP_MULTIPLY" "exec autoexec"
bind "KP_MINUS" "-attack; -attack2;"
bind "KP_PLUS" "alertSentry"
bind "KP_ENTER" "alertThanks"
bind "h" "alertHelp"
bind "v" "alertThanks"  // Marking keys I might want to use in the future
bind "n" "alertThanks"
bind tab +scores

//  Default actions
bind "f" "+use_action_slot_item"
bind "i" "+inspect"
bind "/" "open_charinfo_direct"
bind F3 show_quest_log
bind "'" "+moveup"
bind SEMICOLON "+movedown"
bind MOUSE5 +voicerecord

bind ` toggleconsole
bind F11 toggleconsole

bindtoggle t r_drawviewmodel
